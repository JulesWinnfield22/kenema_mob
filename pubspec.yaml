name: kenema
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.1.1
  dio: ^5.0.0
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  equatable: ^2.0.0 # Specify a version for equatable
  google_fonts: ^6.1.0
  gradient_elevated_button: ^1.0.0
  intl: ^0.19.0
  shared_preferences: ^2.3.1
  flutter_hooks: ^0.20.5
  flutter_svg: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/family_image.png
    - assets/images/pattern_1.png
    - assets/images/kenema.png
    - assets/images/Ad1.png
    - assets/images/Group.png
    - assets/images/nearest.svg
    - assets/images/map.png